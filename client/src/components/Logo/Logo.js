import React from 'react';

import './Logo.css';

const logo = props => (
  <h1 className="logo">
    <span className="logo__icon">🌐</span>
    <span className="logo__text">SocialApp</span>
  </h1>
);

export default logo;
